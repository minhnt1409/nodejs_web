<div class="mt-4">
    <h3> Cap Nhat Thong Tin</h3>

    <form method="post" action="/courses/{{this._id}}?_method=put">
        <div class="form-group">
            <label for="name">Name</label>
            <input type="text" class="form-control" id="name" name="name" value="{{this.name}}">
        </div>
        <div class="form-group">
            <label for="Age">Age</label>
            <input type="text" class="form-control" id="age" name="age" value="{{this.age}}">
        </div>
        <div class="form-group">
            <label for="sex">Sex</label>
            <input type="text" class="form-control" id="sex" name="sex" value="{{this.sex}}">
        </div>

        <button type="submit" class="btn btn-primary">Update</button>
    </form>
</div>
